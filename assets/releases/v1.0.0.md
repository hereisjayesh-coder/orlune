# Orlune v1.0.0 — release metadata

Lightweight record of this release. The installable APK itself lives on the
GitHub Release page, not in this repository — see `README.md`'s "Download"
section for the link.

- **Version:** 1.0.0 (`versionCode = 1`)
- **Tag:** `v1.0.0`
- **Date:** 2026-08-19
- **Status:** Portfolio / Beta
- **Package:** `com.orlune.app`
- **Build variant:** debug-signed (installable directly; no release signing key
  exists yet — see `docs/RELEASE_PROCESS.md`)
- **Verification:** `assembleDebug` + `testDebugUnitTest` (233/233 unit tests)
  both green; installed and launched cleanly on a physical Pixel 7a
  (`versionName`/`versionCode` confirmed via `dumpsys package` and in-app
  Settings → About Orlune); full onboarding flow walked end-to-end on-device.
